// Create exit button.
instance_create_layer(x - 180, 1080 - 330, "Buttons", obj_button_exit);

// Create resume button.
instance_create_layer(x + 180, 1080 - 330, "Buttons", obj_button_resume);

// Create autoshoot button.
instance_create_layer(x, 350, "Buttons", obj_boton_autoshoot);

// Create audio button.
instance_create_layer(x, 900, "Buttons", obj_button_audio);